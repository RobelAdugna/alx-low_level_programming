#include <stdio.h>

/**
 * main - prints the phrase "programming is like building a multilangual puzzle"
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	puts("\programming is like a building a multilingual puzzle");
	return (0);
}
